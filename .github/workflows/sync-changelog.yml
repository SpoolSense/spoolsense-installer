name: Sync changelog to docs site

on:
  push:
    tags:
      - 'v*'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout installer
        uses: actions/checkout@v4

      - name: Checkout docs site
        uses: actions/checkout@v4
        with:
          repository: SpoolSense/spoolsense.org
          token: ${{ secrets.DOCS_PUSH_TOKEN }}
          path: docs-site

      - name: Update installer changelog
        run: |
          cp CHANGELOG.md docs-site/docs/releases/installer.md
          sed -i '1s/.*/# Installer Changelog/' docs-site/docs/releases/installer.md

      - name: Commit and push
        working-directory: docs-site
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/releases/installer.md
          git diff --cached --quiet && exit 0
          git commit -m "docs: sync installer changelog from ${{ github.ref_name }}"
          git push
